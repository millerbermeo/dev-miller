---
import { projects } from '../data/projects';
import CodexIcon from './icons/CodexIcon.astro';
import ClaudeCodeIcon from './icons/ClaudeCodeIcon.astro';
import OpenCodeIcon from './icons/OpenCodeIcon.astro';
import OpenClawIcon from './icons/OpenClawIcon.astro';

const copilotIcons: Record<string, any> = {
  "Codex": CodexIcon,
  "Claude Code": ClaudeCodeIcon,
  "OpenCode": OpenCodeIcon,
  "OpenClaw": OpenClawIcon,
};

const accentMap: Record<string, string> = {
  indigo: "border-indigo-500/30 text-indigo-400 bg-indigo-500/10",
  emerald: "border-emerald-500/30 text-emerald-400 bg-emerald-500/10",
  violet: "border-violet-500/30 text-violet-400 bg-violet-500/10",
  orange: "border-orange-500/30 text-orange-400 bg-orange-500/10",
  cyan: "border-cyan-500/30 text-cyan-400 bg-cyan-500/10",
  green: "border-green-500/30 text-green-400 bg-green-500/10",
};

// Gradiente de fallback por proyecto: se ve detrás de la imagen y queda
// visible con estilo si la captura no existe (imágenes pendientes).
const placeholderMap: Record<string, string> = {
  indigo: "from-indigo-500/40 to-violet-600/30",
  emerald: "from-emerald-500/40 to-cyan-600/30",
  violet: "from-violet-500/40 to-fuchsia-600/30",
  orange: "from-orange-500/40 to-amber-600/30",
  cyan: "from-cyan-500/40 to-blue-600/30",
  green: "from-green-500/40 to-emerald-600/30",
};
---

<section id="projects" class="py-16 sm:py-24 px-4 sm:px-6">
  <div class="max-w-4xl mx-auto">
    <p
      class="text-xs font-semibold uppercase tracking-widest text-accent mb-3"
    >
      Proyectos
    </p>
    <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-fg mb-3 sm:mb-4">
      Trabajo destacado
    </h2>
    <p class="text-muted mb-8 sm:mb-12 text-sm sm:text-[15px]">
      Proyectos reales construidos en producción. Cada uno resolvió un problema
      concreto de negocio.
    </p>

    <div class="space-y-5 sm:space-y-6">
      {
        projects.map((project) => (
          <article
            data-anim="project-card"
            data-spotlight
            class="group bg-fg/[.03] border border-line/10 rounded-2xl overflow-hidden hover:border-line/20 hover:-translate-y-1 transition-all duration-300"
          >
            <div class="flex flex-col lg:flex-row">
              <!-- Image (con placeholder de gradiente como fallback si no existe) -->
              <div
                class={`relative w-full h-44 sm:h-52 lg:w-64 lg:h-auto lg:min-h-[13rem] overflow-hidden flex-shrink-0 bg-gradient-to-br ${placeholderMap[project.accent]}`}
              >
                <!-- Fallback: inicial del proyecto + brillo -->
                <div
                  class="absolute inset-0 flex items-center justify-center"
                  aria-hidden="true"
                >
                  <span class="font-display text-6xl font-bold text-white/25 select-none">
                    {project.title.charAt(0)}
                  </span>
                </div>
                <img
                  src={`/${project.image}`}
                  alt={project.title}
                  width="256"
                  height="208"
                  loading="lazy"
                  decoding="async"
                  onerror="this.style.display='none'"
                  class="relative w-full h-full object-cover group-hover:scale-105 transition-transform duration-500 opacity-90 group-hover:opacity-100"
                />
              </div>

              <!-- Content -->
              <div class="flex-1 p-5 sm:p-6 min-w-0">
                <!-- Label -->
                <span
                  class={`inline-flex items-center px-2.5 py-1 text-xs font-semibold rounded-full border mb-3 ${accentMap[project.accent]}`}
                >
                  {project.label}
                </span>

                <h3 class="text-base sm:text-lg font-bold text-fg mb-3 sm:mb-4">
                  {project.title}
                </h3>

                <div class="space-y-2.5 sm:space-y-3 mb-4 sm:mb-5">
                  <div class="flex flex-col sm:flex-row sm:gap-2 text-sm">
                    <span class="text-faint font-medium sm:flex-shrink-0 sm:w-20 mb-0.5 sm:mb-0">
                      Problema
                    </span>
                    <span class="text-muted text-xs sm:text-sm">{project.problem}</span>
                  </div>
                  <div class="flex flex-col sm:flex-row sm:gap-2 text-sm">
                    <span class="text-faint font-medium sm:flex-shrink-0 sm:w-20 mb-0.5 sm:mb-0">
                      Solución
                    </span>
                    <span class="text-muted text-xs sm:text-sm">{project.solution}</span>
                  </div>
                  <div class="flex flex-col sm:flex-row sm:gap-2 text-sm">
                    <span class="text-faint font-medium sm:flex-shrink-0 sm:w-20 mb-0.5 sm:mb-0">
                      Resultado
                    </span>
                    <span class="text-fg/80 font-medium text-xs sm:text-sm">
                      {project.result}
                    </span>
                  </div>
                </div>

                <!-- Footer -->
                <div class="space-y-3 pt-3 sm:pt-4 border-t border-line/[.08]">
                  <div class="flex flex-wrap gap-1.5">
                    {project.stack.map((tech) => (
                      <span class="px-2 py-0.5 text-xs text-muted bg-fg/[.03] border border-line/10 rounded-md">
                        {tech}
                      </span>
                    ))}
                  </div>
                  {project.copilots && project.copilots.length > 0 && (
                    <div class="flex items-center gap-2 flex-wrap">
                      <span class="text-xs text-faint font-medium">AI Copilot:</span>
                      {project.copilots.map((copilot: string) => {
                        const IconComponent = copilotIcons[copilot];
                        return (
                          <span class="inline-flex items-center gap-1 px-2 py-0.5 text-xs text-cyan-400 bg-cyan-500/10 border border-cyan-500/20 rounded-md">
                            {IconComponent && <IconComponent />}
                            {copilot}
                          </span>
                        );
                      })}
                    </div>
                  )}
                  <div class="flex items-center justify-between flex-wrap gap-3 pt-2">
                    <div class="flex items-center gap-3">
                      {project.deploy && (
                        <a
                          href={project.deploy}
                          target="_blank"
                          rel="noopener noreferrer"
                          class="flex items-center gap-1.5 text-xs font-medium text-muted hover:text-fg transition-colors duration-200 flex-shrink-0"
                        >
                          <ion-icon name="globe-outline" class="text-sm" />
                          Deploy
                        </a>
                      )}
                      {project.github && (
                        <a
                          href={project.github}
                          target="_blank"
                          rel="noopener noreferrer"
                          class="flex items-center gap-1.5 text-xs font-medium text-muted hover:text-fg transition-colors duration-200 flex-shrink-0"
                        >
                          <ion-icon name="logo-github" class="text-sm" />
                          GitHub
                        </a>
                      )}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </article>
        ))
      }
    </div>

    <!-- GitHub CTA -->
    <div class="mt-7 sm:mt-8 text-center">
      <a
        href="https://github.com/millerbermeo"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 px-5 py-2.5 text-sm font-medium text-muted hover:text-fg bg-fg/[.03] hover:bg-fg/[.06] border border-line/[.12] rounded-lg transition-all duration-200"
      >
        <ion-icon name="logo-github" class="text-base"></ion-icon>
        Ver más en GitHub
      </a>
    </div>
  </div>
</section>
